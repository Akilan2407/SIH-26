import { NavLink, Outlet } from "react-router-dom";
import { Activity, Bot, BrainCircuit, ClipboardList, FileText, LayoutDashboard, Menu, ScanEye, Settings2, Sparkles, Wifi } from "lucide-react";
const links = [["/", "Overview", LayoutDashboard], ["/screening", "New Screening", ScanEye], ["/analysis", "Analysis", Activity], ["/explainability", "Explainability", Sparkles], ["/assistant", "AI Assistant", Bot], ["/reports", "Reports", FileText], ["/tracking", "ML Tracking", BrainCircuit]];
export default function Layout() { return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark"><ScanEye size={20} /></span><span>retina<span>care</span></span></div><p className="eyebrow">SCREENING PLATFORM</p><nav>{links.map(([to, label, Icon]) => <NavLink key={to} to={to} end={to === "/"}><Icon size={18} /><span>{label}</span></NavLink>)}</nav><div className="sidebar-foot"><div className="status-dot"><Wifi size={15} /> Mock API active</div><div className="privacy"><Settings2 size={15} /><span>Privacy first<br /><small>No patient data stored</small></span></div></div></aside><main className="main"><header className="topbar"><button className="icon-button mobile-menu" aria-label="Open menu"><Menu size={20} /></button><div><span className="topbar-label">CURRENT SESSION</span><strong>DR-0001 <span className="live-dot" /> Ready</strong></div><div className="top-actions"><button className="icon-button" title="System status"><Wifi size={18} /></button><div className="profile">SA<span>Screening analyst</span></div></div></header><div className="content"><Outlet /></div></main></div>; }