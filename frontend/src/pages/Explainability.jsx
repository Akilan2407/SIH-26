import { useEffect, useState } from "react";
import { AlertCircle, Eye, Info, Sparkles } from "lucide-react";
import { getGradCAM, getSHAP } from "../api/explainabilityApi.js";
import { useScreening } from "../hooks/useScreening.js";
export default function Explainability() { const { result, image, file } = useScreening(); const [tab, setTab] = useState("gradcam"); const [explanation, setExplanation] = useState(null); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); useEffect(() => { if (!file) return; setLoading(true); getGradCAM(file).then(setExplanation).catch((err) => setError(err.message)).finally(() => setLoading(false)); }, [file]); const label = result?.predictedLabel || "Awaiting model result"; return <><div className="page-heading compact"><div><p className="eyebrow">EXPLAINABILITY</p><h1>Why did the AI say that?</h1><p className="lede">Transparent visual context for both patients and reviewers.</p></div><div className="tab-switch"><button className={tab === "gradcam" ? "selected" : ""} onClick={() => setTab("gradcam")}><Eye size={15} /> Grad-CAM</button><button className={tab === "shap" ? "selected" : ""} onClick={() => { setTab("shap"); getSHAP(result?.screeningId || "DR-0001").then(setExplanation); }}><Sparkles size={15} /> SHAP</button></div></div>{tab === "gradcam" ? <><div className="explain-grid"><Visual title="Original fundus image" src={explanation?.originalImage || image || "/fundus-placeholder.svg"} /><Visual title="Grad-CAM heatmap" src={explanation?.gradCam || "/fundus-placeholder.svg"} /><Visual title="Overlay" src={explanation?.overlay || "/fundus-placeholder.svg"} overlay /></div>{loading && <div className="inline-success">Generating AI explanation...</div>}<div className="explain-caption"><Info size={17} /><div><strong>Model-attention visualization</strong><p>Grad-CAM highlights regions that contributed strongly to the model's prediction. This is an explanation of model attention, not a confirmed lesion map or clinical diagnosis.</p></div></div>{error && <div className="inline-error">{error}</div>}</> : <div className="empty-state"><AlertCircle size={25} /><h2>SHAP explanation not available</h2><p>SHAP values will appear here when the LightGBM tabular model returns them from the backend.</p><span>Awaiting tabular model result</span></div>}<div className="mini-result"><span className="eyebrow">RETINAL IMAGE MODEL</span><strong>{label}</strong><span>{result?.confidence == null ? "Awaiting model result" : `Model confidence: ${Math.round(result.confidence * 100)}%`}</span></div></>; }
function Visual({ title, src, overlay }) { return <div className="visual-card"><div className="visual-head"><span>{title}</span><span className="visual-chip">{overlay ? "ATTENTION" : "INPUT"}</span></div><div className="visual"><img src={src} alt={title} /></div></div>; }