import { useState } from "react";
import { Download, FileText, Printer, Sparkles } from "lucide-react";
import { useScreening } from "../hooks/useScreening.js";
import { generateReport } from "../api/reportApi.js";
export default function Reports() { const { result, image } = useScreening(); const [status, setStatus] = useState(""); const create = async () => { setStatus("Generating report..."); try { const report = await generateReport(result?.screeningId || "DR-0001"); setStatus(report.pdfUrl ? "Report ready" : "Report generated. PDF unavailable in mock mode."); } catch (error) { setStatus(error.message); } }; return <><div className="page-heading compact"><div><p className="eyebrow">REPORTS</p><h1>Screening report</h1><p className="lede">A reviewable summary for the screening workflow.</p></div><button className="button primary" onClick={create}><FileText size={16} /> Generate report</button></div><div className="report-preview"><div className="report-top"><div><span className="report-logo">retinacare</span><p>AI-ASSISTED SCREENING REPORT</p></div><span>DR-0001 · 19 Aug 2026</span></div><div className="report-title"><div><p className="eyebrow">RETINAL IMAGE MODEL</p><h2>{result?.predictedLabel || "Not available"}</h2><span>{result?.confidence == null ? "Model confidence: Not available" : `Model confidence: ${Math.round(result.confidence * 100)}%`}</span></div><div className="report-icon"><Sparkles size={25} /></div></div><div className="report-details"><div><span>FUNDUS IMAGE</span>{image ? <img src={image} alt="Fundus" /> : <div className="not-available">Not available</div>}</div><div><span>GRAD-CAM</span><div className="not-available">Not available</div></div><div><span>TABULAR MODEL</span><div className="not-available">Not available</div></div></div><div className="report-foot">This AI-assisted screening is not a medical diagnosis and does not replace professional ophthalmic evaluation.</div></div>{status && <div className="inline-success">{status}</div>}<div className="report-actions"><button className="button secondary" onClick={create}><Printer size={16} /> View PDF</button><button className="button secondary" onClick={() => setStatus("PDF download is available after backend generation.")}><Download size={16} /> Download PDF</button></div></>; }